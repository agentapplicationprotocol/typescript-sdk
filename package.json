{
  "name": "typescript-sdk",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "pnpm -r dev",
    "prepare": "husky"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "@vitest/coverage-v8": "^4.1.2",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "oxfmt": "^0.42.0",
    "typescript": "^5.0.0",
    "vitest": "^4.1.2"
  },
  "lint-staged": {
    "*.{ts,json,md}": "oxfmt"
  }
}
